apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "teamcity-operator.fullname" . }}-serving-cert
  labels:
  {{- include "teamcity-operator.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "teamcity-operator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "teamcity-operator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "teamcity-operator.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert