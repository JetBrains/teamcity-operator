apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "teamcity-operator.fullname" . }}-selfsigned-issuer
  labels:
  {{- include "teamcity-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}